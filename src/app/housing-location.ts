// this ia na interface
//we are creating an object of the interface type
export interface HousingLocation {
    name: string,
    city: string,
    state: string,
    photo: string,
    availableUnits: number,
    wifi: boolean,
    laundry: boolean
}
